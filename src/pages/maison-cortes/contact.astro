---
import Shell from '~/tenants/maison-cortes/ui/Shell.astro';
import { maisonCortesCopy } from '~/tenants/maison-cortes/copy';

const metadata = {
  title: 'MAISON CORTES — Contact',
  description: 'Contact.',
  ignoreTitleTemplate: true,
};
---

<Shell metadata={metadata} title="Contact">
  <section class="mc-section">
    <a href={`mailto:${maisonCortesCopy.contactEmail}`}>{maisonCortesCopy.contactEmail}</a>
  </section>
</Shell>
